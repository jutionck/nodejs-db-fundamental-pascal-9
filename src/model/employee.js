const Employee = (id, firstName, lastName, bod, pob, address, balance) => {
    return {
        id,
        firstName,
        lastName,
        bod,
        pob,
        address,
        balance,
    }
}

module.exports = Employee;
