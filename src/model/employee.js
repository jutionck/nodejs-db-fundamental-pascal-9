const Employee = (id, firstName, lastName, bod, pob, address) => {
    return {
        id,
        firstName,
        lastName,
        bod,
        pob,
        address,
    }
}

module.exports = Employee;
